import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'boutique-hotel-technikum-app',
  webDir: 'dist'
};

export default config;
